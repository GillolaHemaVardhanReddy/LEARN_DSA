# deps / build
node_modules/
dist/
build/
__pycache__/
*.pyc
target/

# reference books — large + copyrighted, kept LOCAL only.
# The tracked artifact is references/README.md (the map); the PDFs stay off git.
references/*.pdf
references/*.crdownload
references/*.epub

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code local (machine-specific, not shared)
.claude/settings.local.json

# SECRETS — never commit. .mcp.json holds a live LEETCODE_SESSION login token.
.mcp.json
*.session
.env
.env.*
