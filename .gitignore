# deps / build
node_modules/
dist/
build/
__pycache__/
*.pyc
target/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code local (machine-specific, not shared)
.claude/settings.local.json

# SECRETS — never commit. .mcp.json holds a live LEETCODE_SESSION login token.
.mcp.json
*.session
.env
.env.*
