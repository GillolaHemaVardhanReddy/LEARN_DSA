---
description: Run what's due on the retrieval ladder (closed-book, mixed, marks only)
---
Read `dashboard/REVISION_QUEUE.md`. Run what is due today or overdue, closed-book: cards (the 4 disqualifier answers → pattern + trigger + idea + complexity-with-the-work) and, in a weekend block, cold re-solves (core loop / LC from memory). Seed every statement with 2–3 examples; a statement clarification is allowed and not scored; marks only until the end. The RECALL slot takes the 2 oldest items (10 min); the rest **slide** a day, never drop; a backlog > 10 is collapsed to one item per topic with the others marked "skipped" to their next rung — written in the queue, never silent. Pass → next rung (+1d in-BUILD card → +7d re-solve → +30d → +90d; felt-hard repeats +7d); fail → +1d and a `dashboard/MISTAKES.md` note (`V#n`) if it's a root cause; a ❌ on an L4/L5 topic follows the regression rule (PLAN §5). Update the queue (Due / Ladder / Completed), then `/endsession`.
