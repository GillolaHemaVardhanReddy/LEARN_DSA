---
description: Run what's due on the retrieval ladder (closed-book, mixed, marks only)
---
Read `dashboard/REVISION_QUEUE.md`. Run everything due today or overdue, closed-book: cards (name the pattern + trigger + idea + complexity-with-the-work) and cold re-solves (core loop / LC from memory). Seed every statement with 2–3 examples; disqualifier gate first; marks only until the end. More than 3 due → prune to the oldest per topic (the queue gets pruned, boss doesn't; cap 10 min unless he asks for more). Pass → next ladder stage (+1d → +3d → +7d → +30d → +90d; felt-hard stays); fail → +1d and a `MISTAKES.md` note if it's a root cause. Update `REVISION_QUEUE.md` (Due / Ladder / Completed), then `/endsession`.
