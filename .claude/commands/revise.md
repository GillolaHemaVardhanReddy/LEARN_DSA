---
description: Run a spaced-repetition revision session
---
Run a revision session per `CLAUDE.md` §8. Read `REVISION_QUEUE.md`, take everything due
today or overdue, and run it **closed-book first** — make me reconstruct the idea/template
before you show anything. **Interleave** topics rather than batching one. Score each as
pass/fail. Then update `REVISION_QUEUE.md` and `PROGRESS.md`: a pass advances the interval
(1d→3d→7d→14d→30d→90d); a fail resets to +1d and flags the topic weak (and may warrant a
`MISTAKE_JOURNAL.md` entry). Commit at the end.
