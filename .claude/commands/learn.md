---
description: Open a new topic (day-1 protocol) — derive it from the previous tool's bottleneck
argument-hint: "<NN-topic>  e.g. 06-sorting"
---
Open topic `$ARGUMENTS` per `dashboard/PLAN.md` §4.5 and `dashboard/SYSTEM.md` §3. Create `practice/<NN-topic>/` only now (never ahead).
1. **Probe** dormant knowledge closed-book (2–3 questions). Teach only into the gaps.
2. **Derive:** state the previous tool's bottleneck on a 5-element example; boss derives the new tool via atomic Q&A (one question, then stop). Never lecture. If the topic's core is an invariant, scaffold the bug that violates it and let his bug teach it.
3. **Template from memory** (closed-book) → L2/L3 evidence.
4. Scaffold **ONE** LEARN problem (`learn/01-…/solution.cpp`: BRUTE → BRIDGE with question-holes → OPTIMAL, real LC signature via MCP). He solves it; LC verdict is truth.
5. Kira writes `notes.md` **after** his derivation (teaching-BITS format: why-first, hand-run trace, unlock, recipe, traps, "where it runs in the world", "derived from / reduces to"). Point to the Striver video(s) in `dashboard/resources/striver-videos.md` as consolidation.
6. Log: PROGRESS level with evidence · REVISION_QUEUE (+1d card) · README TODAY resume line. Commit via `/endsession`.
