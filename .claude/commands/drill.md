---
description: 5 mixed recognition cards — name the pattern only (no coding), disqualifier gate first; TEST mode, logged to tests/mixed/
argument-hint: "[optional: focus topic or count]"
---
Per `dashboard/PLAN.md` §5 and `dashboard/SYSTEM.md` §4: give 5 fresh disguised statements (or `$ARGUMENTS` count/focus), drawn from topics at L3+, mixed families with at least one adjacent-family trap, never two consecutive from the same family. Every statement seeded with 2–3 input→output lines, no LC titles. For each, boss answers: the 4 disqualifier answers → pattern → trigger → complexity as a sentence with the work in it. TEST mode: no hints; a statement clarification is allowed and not scored. Marks only during the run, full reveal after the last card. Log the run to `tests/mixed/<YYYY-MM-DD>-drill.md` (statements + marks, no key). A ❌ or 🟡 → `dashboard/MISTAKES.md` (`V#n`, root cause + re-test) and a +1d card in `dashboard/REVISION_QUEUE.md`; a ❌ on an L4/L5 topic follows the regression rule (PLAN §5). A ✅ on an unseen statement ≥14 days after the topic's checkpoint pass = L5 evidence in `dashboard/PROGRESS.md` (base + ≥2 variants across ≥2 tests). Never reveal a pattern name mid-run. Commit via `/endsession`.
