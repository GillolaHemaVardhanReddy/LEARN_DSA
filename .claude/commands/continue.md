---
description: Resume the DSA program where we left off
---
Resume the DSA program. Read `PROGRESS.md` and `REVISION_QUEUE.md`, render the dashboard
(`CLAUDE.md` §7), then continue from the current topic and the "next highest-ROI step"
recorded in `PROGRESS.md`. If anything is due or overdue in `REVISION_QUEUE.md` today,
fold that in first (closed-book). If `PROGRESS.md` is still the empty template, run First
Run instead (`CLAUDE.md` §14).
