---
description: Friday cold checkpoint on the current topic (+2 old cards) — the L4 evidence; also the re-entry checkpoint in Tier 0
argument-hint: "[optional: <NN-topic>]"
---
Build and run the checkpoint per `dashboard/PLAN.md` §4.2/§4.5 and `dashboard/SYSTEM.md` §4, in `tests/checkpoints/<NN-topic>-<YYYY-MM-DD>/` (no answer key in the folder).
- **Re-entry topics (Tier 0) start with Q0 — derive it:** what bottleneck does this tool kill, what does it reduce to, where does it run in the world. Then the 3 Qs.
- **Q1** a fresh disguised statement seeded with 2–3 examples → the 4 disqualifier answers, the pattern, the trigger, the complexity with the line behind each factor. **Q2** the template / core loop from memory (10 min). **Q3** one unseen medium coded cold → LC verdict, **time box = the BUILD slot** (30 min in month 1, 45–60 after day 30), 0 hints; out of time ⇒ finish it first thing Saturday AM (still passes if AC within 60 total minutes, no hint). Plus 2 cards from older topics (different families, one adjacent-family trap). **Topic 00 uses its own format** (`tests/checkpoints/00-complexity-2026-09-11/`: 6 algorithms to bound, pass = 6/6 with the line named).
- TEST mode: no hints (a hint costs signal and is logged); a statement clarification is allowed and not scored. Marks only during the run, full reveal after.
- **Bar: 3/3 = pass** → L4 evidence in `dashboard/PROGRESS.md` (a re-entry checkpoint confirms L4 at most), topic enters the ladder (+14d/+45d/+90d cards). **2/3 ⇒ only the failed question is re-tested next Friday, and no new topic opens until it passes**; log the miss in `dashboard/MISTAKES.md` (`V#n`) with a re-test. ≤1/3 ⇒ the topic reopens with a shortened Day 1.
- Collect the 5-minute weekly self-report (SYSTEM.md §7d) and review the running experiment (§7b) → `dashboard/LEARNING_PROFILE.md`.
- Update PROGRESS / REVISION_QUEUE / README TODAY, then `/endsession`.
