---
description: Log the mistake we just hit, with root cause and a re-test
---
Log the mistake we just hit into `MISTAKE_JOURNAL.md` using the entry template there:
the wrong move I made, the **root cause** (the real mental-model error — not "careless"),
the correct understanding, a prevention rule (a checklist item), a similar **unseen**
re-test problem, and a re-attempt date. Also add that re-test to `REVISION_QUEUE.md`. If
this root cause has appeared before, promote it to the recurring-mistake watchlist and
add it to the pre-submit checklist. Commit at the end.
